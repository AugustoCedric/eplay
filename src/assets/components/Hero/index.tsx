import bannerImage from '../../images/fundo_hogwarts.png'
import { Banner } from './styles'

const Hero = () => (
  <Banner style={{ background: `url(${bannerImage})` }}>hero</Banner>
)

export default Hero
